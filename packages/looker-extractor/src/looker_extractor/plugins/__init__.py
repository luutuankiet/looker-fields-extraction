"""Reference plugin implementations bundled with looker-extractor.

Third-party plugins live in their own pip packages and are discovered via
the ``looker_extractor.plugins`` entry-points group (wired in a follow-up
iteration). Bundled plugins discoverable through the same mechanism live
here as submodules.
"""
