"""Core: HTTP client, config, swagger loader, manifest loader, output writers.

Plugin-agnostic infrastructure. Plugins depend on ``looker_extractor.core.*``
for their HTTP/config/loader needs but do NOT depend on each other.
"""
