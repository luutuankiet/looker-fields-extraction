"""looker-fields: Extract entity metadata from any Looker instance via the API.

Post-pivot vision: meltano-for-looker — a passthru extractor that takes API
responses and emits them as semi-structured (JSONL / Parquet) data ready for
warehouse ingestion. No projection, no dedup, no opinionated transforms.
Do one thing well: extract + emit.
"""

__version__ = "0.3.0a0"
